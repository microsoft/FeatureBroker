// Copyright (c) Microsoft Corporation.
// Licensed under the MIT License.
#include <map>
#include <string>
#include <cpprest/http_client.h>
#include <cpprest/uri.h>

#include <vw_remote_model/inference_request.hpp>

namespace resonance_vw {

    /*
    APSInferenceRequest(std::map<std::string, std::string> const& featureMap, std::string const& baseUri,
                        std::string const& experimentId)
        : m_featureMap(featureMap), m_baseUri(baseUri), m_experimentId(experimentId) {}

    void APSInferenceRequest::GetResponse() {
        //web::http::uri_builder builder(m_baseUri);
        web::http::client::http_client client(U("http://www.bing.com"));

        //http_client clie pplx::task<void> requestTask =
            //fstream web::http::client client

    }
    */

}
